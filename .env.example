# Environnement local par défaut (Docker compose infra/docker-compose.yml)
# Remplissez/dupliquez en .env si nécessaire

# Auth API
NOTEBOOK_GENERATION_AUTH=changeme

# PostgreSQL (pgvector)
POSTGRES_DSN=postgres://notebook:notebook@localhost:5432/notebook

# Ollama
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Modèles requis (à adapter)
# OLLAMA_EMBED_MODEL=nomic-embed-text
# OLLAMA_LLM_MODEL=llama3:instruct

# Journalisation
LOG_SAMPLE_PCT=10
