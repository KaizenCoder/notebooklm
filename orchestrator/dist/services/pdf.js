export function createPdf(_env) {
    return {
        async extractText(_url) {
            // MOV: extraction PDF réelle sera intégrée au lot 16 ; pour MOV on ne traite que le texte direct
            return '';
        }
    };
}
